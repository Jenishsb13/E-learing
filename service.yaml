apiVersion: v1
kind: Service
metadata:
  name: crud-app-service
  namespace: elearning

spec:
  type: LoadBalancer

  selector:
    app: crud-app

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
